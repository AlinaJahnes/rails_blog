<%= link_to "Home", root_path %>
<% if current_user %>
<!-- It's best practice to refer to instance variables instead of method names. Also if you move this method out of the application controller helper (which you should), you won't have access to it anymore!-->
  <%= @current_user.username%>
  <%= link_to "Profile", user_path(session[:id]) %>
  <%= link_to "Logout", logout_path %>
<% else %>
  <%= link_to "Sign up", signup_path %>
  <%= link_to "Login", login_path %>
<% end %>
